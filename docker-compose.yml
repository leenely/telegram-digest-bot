version: "3.9"

services:
  digest-bot:
    build: .
    container_name: digest-bot
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
